{
  "name": "format-phone-number",
  "version": "0.0.11",
  "description": "Make correct combination of phone number parts depending on a country.",
  "keywords": [
    "format",
    "phone",
    "number"
  ],
  "author": {
    "name": "Ross Khanas",
    "email": "rk@andcards.com"
  },
  "scripts": {
    "clean": "rimraf lib",
    "lint": "eslint src",
    "test": "mocha --recursive --compilers js:babel-core/register",
    "build": "npm run lint && npm test && npm run clean && babel src --out-dir lib"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:opensource-cards/format-phone-number.git"
  },
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "dependencies": {
    "mask-parser": "^0.0.1"
  },
  "devDependencies": {
    "babel": "^6.3.26",
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-eslint": "^8.0.0",
    "babel-preset-es2015": "^6.9.0",
    "eslint": "^4.9.0",
    "eslint-config-airbnb": "^17.0.0",
    "eslint-plugin-import": "^2.2.0",
    "mocha": "^5.0.0",
    "rimraf": "^2.5.3"
  }
}
